# 課題

## 問題

簡単なログイン画面を作成し、「ログイン」ボタンをクリックしたら`/dashboard`へ移動してください。

## 条件

* `/` にLoginページを表示
* `/dashboard` にDashboardページを表示
* Loginページに「ログイン」ボタンを配置
* ボタンをクリックしたら`/dashboard`へ移動
* `useNavigate`を使用する
